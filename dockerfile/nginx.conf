server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index home.html;
    }

    location /metrics {
        stub_status;
        allow all;  # 葛电 IP 林家 立辟 倾侩
    }
}

