FROM nginx:alpine

WORKDIR /app

COPY *.jar /usr/share/nginx/html/app.jar

COPY default.conf /etc/nginx/conf.d/default.conf
#COPY default.conf /etc/nginx/nginx.conf

CMD ["nginx", "-g", "daemon off;"]

EXPOSE 80
